#version 460

layout(local_size_x = 1, local_size_y = 1) in;


//layout(binding = 0) uniform readonly image2D inputState;
layout(binding = 1, RG32F) uniform writeonly image2D outputState;

void main() {
    ivec2 coord = gl_GlobalInvocationID.xy;
    imageStore(outputState, ivec2(0,0), vec4(1, 0, 0, 0));
}